extends ../layout

block main
    label(for='username') Username
    input#username(type='text' name='username' value=user.username)
    br
    br
    button#edit(onclick=`editUserRequest();`) Save
    script.
      let userObj = !{JSON.stringify(user)};



